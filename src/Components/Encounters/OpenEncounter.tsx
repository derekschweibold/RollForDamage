import React from 'react';
import IEncounter from '../../Interfaces/IEncounter';

interface IOpenEncounterProps {
	encounter: IEncounter;
}

const OpenEncounter: React.FC<IOpenEncounterProps> = ({ encounter }) => {
	return <></>;
};

export default OpenEncounter;
